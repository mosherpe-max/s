
{
  "entities": {
    "BuyerProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BuyerProfile",
      "type": "object",
      "description": "Represents a patron or customer using the Koop app to place orders.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BuyerProfile entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the buyer."
        },
        "email": {
          "type": "string",
          "description": "The email address of the buyer.",
          "format": "email"
        },
        "currentLatitude": {
          "type": "number",
          "description": "The current latitude of the buyer's device for map display and delivery tracking."
        },
        "currentLongitude": {
          "type": "number",
          "description": "The current longitude of the buyer's device for map display and delivery tracking."
        },
        "subscriberStatus": {
          "type": "boolean",
          "description": "Indicates if the buyer has an active subscription or special status, updated upon successful payment."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the buyer profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the buyer profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "subscriberStatus",
        "createdAt",
        "updatedAt"
      ]
    },
    "Seller": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Seller",
      "type": "object",
      "description": "Represents a beverage cart driver or vendor managing menus and fulfilling orders.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Seller entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the seller or venue."
        },
        "email": {
          "type": "string",
          "description": "The email address of the seller for contact and login.",
          "format": "email"
        },
        "contactPhone": {
          "type": "string",
          "description": "The primary contact phone number for the seller."
        },
        "currentLatitude": {
          "type": "number",
          "description": "The current latitude of the seller's beverage cart for real-time tracking."
        },
        "currentLongitude": {
          "type": "number",
          "description": "The current longitude of the seller's beverage cart for real-time tracking."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the seller profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the seller profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "contactPhone",
        "createdAt",
        "updatedAt"
      ]
    },
    "SellerPrivateConfig": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SellerPrivateConfig",
      "type": "object",
      "description": "Stores sensitive venue configuration like payment API keys. Controlled by Admin only.",
      "properties": {
        "id": { "type": "string" },
        "authorizeNetLoginId": { "type": "string" },
        "authorizeNetTransactionKey": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Defines a generic item that can be sold, such as a beverage or snack.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the product (e.g., 'Bottled Water', 'Craft Beer A')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product."
        },
        "category": {
          "type": "string",
          "description": "The category the product belongs to (e.g., 'Beer', 'Spirits', 'Soft Drinks', 'Snacks')."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL to an image of the product.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the product was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the product was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "category",
        "createdAt",
        "updatedAt"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a customer's placed order for items from a specific seller.",
      "properties": {
        "id": {
          "type": "string"
        },
        "buyerProfileId": {
          "type": "string"
        },
        "sellerId": {
          "type": "string"
        },
        "orderDateTime": {
          "type": "string",
          "format": "date-time"
        },
        "status": {
          "type": "string"
        },
        "totalAmount": {
          "type": "number"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "buyerProfileId",
        "sellerId",
        "status",
        "totalAmount",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/buyerProfile",
        "definition": {
          "entityName": "BuyerProfile",
          "schema": {
            "$ref": "#/backend/entities/BuyerProfile"
          },
          "description": "Stores private profile data for each buyer."
        }
      },
      {
        "path": "/sellers/{sellerId}",
        "definition": {
          "entityName": "Seller",
          "schema": {
            "$ref": "#/backend/entities/Seller"
          },
          "description": "Stores core seller profiles. Publicly readable for maps and discovery."
        }
      },
      {
        "path": "/sellers_private/{sellerId}",
        "definition": {
          "entityName": "SellerPrivateConfig",
          "schema": {
            "$ref": "#/backend/entities/SellerPrivateConfig"
          },
          "description": "Vault for sensitive venue credentials. Accessible only to Koop Admins and Cloud Functions."
        }
      },
      {
        "path": "/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Centralized collection for all customer orders."
        }
      }
    ]
  }
}
