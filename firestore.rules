rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Insecure: Allow read/write access to all documents by anyone.
    // This is for prototyping only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
